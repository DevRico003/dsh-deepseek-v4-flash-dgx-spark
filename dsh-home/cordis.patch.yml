# Playwright MCP disabled on 2026-08-20: DSH Computer Use (computer_observe/computer_action)
# now covers real browsing, and two browser stacks confused the agent. dsh-preview keeps the
# self-verification loop. Re-enable by restoring the row from git history of the setup repo.
[]
