# Home-level composition patch. Empty on purpose: the Playwright MCP row that once lived here
# was removed on 2026-08-20 (a second browser stack next to dsh-preview confused the agent);
# ui_snapshot (dsh-verifier) and dsh-preview cover headless browsing. Restore a row from the
# git history of the setup repo if you need the MCP again.
[]
